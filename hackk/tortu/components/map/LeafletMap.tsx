"use client"; import "leaflet/dist/leaflet.css"; import L from "leaflet"; import { MapContainer, Marker, Popup, TileLayer } from "react-leaflet"; import Link from "next/link"; import { KAPADOKYA_CENTER } from "@/lib/constants"; import type { Listing } from "@/lib/types";
const icon=new L.Icon({iconUrl:"https://unpkg.com/leaflet@1.9.4/dist/images/marker-icon.png",shadowUrl:"https://unpkg.com/leaflet@1.9.4/dist/images/marker-shadow.png",iconSize:[25,41],iconAnchor:[12,41]});
export default function LeafletMap({listings}:{listings:Listing[]}){ return <MapContainer center={KAPADOKYA_CENTER} zoom={8} className="h-[420px] rounded-lg" scrollWheelZoom={false}><TileLayer attribution='&copy; OpenStreetMap contributors' url="https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png"/>{listings.map(l=><Marker key={l.id} position={[l.pickup_latitude,l.pickup_longitude]} icon={icon}><Popup><b>{l.title}</b><br/>{l.pickup_city}<br/><Link href={`/listings/${l.id}`}>Detay</Link></Popup></Marker>)}</MapContainer>; }
