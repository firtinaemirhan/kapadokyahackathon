import { NextResponse } from "next/server"; import { pdf } from "@react-pdf/renderer"; import { CertificatePDF } from "@/components/certificate/CertificatePDF"; import { transactions } from "@/lib/demo-data";
export async function GET(_:Request,{params}:{params:{id:string}}){ try{const tx=transactions.find(i=>i.id===params.id); const blob=await pdf(<CertificatePDF id={params.id} co2={tx?.co2_kg??17.4}/>).toBlob(); return new NextResponse(blob,{headers:{"Content-Type":"application/pdf","Content-Disposition":`attachment; filename="tortu-${params.id}.pdf"`}});}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"certificate failed"},{status:500})} }
