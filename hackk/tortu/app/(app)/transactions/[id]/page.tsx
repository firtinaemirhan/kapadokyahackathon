import { Button } from "@/components/ui/button"; import { Card } from "@/components/ui/card"; import { transactions } from "@/lib/demo-data";
export default function TransactionDetailPage({params}:{params:{id:string}}){ const tx=transactions.find(i=>i.id===params.id)??transactions[0]; return <div className="mx-auto max-w-3xl px-4 py-10"><Card className="space-y-4 p-6"><h1 className="font-serif text-3xl font-semibold">İşlem {tx.id}</h1><p>Durum: {tx.status}</p><p>Mesafe: {tx.distance_km} km · CO₂: {tx.co2_kg} kg</p><Button asChild><a href={`/api/certificates/${tx.id}`}>PDF sertifika indir</a></Button></Card></div>; }
