export async function findIndustrialPois(lat:number,lng:number,radius=25000){ const query=`[out:json][timeout:25];(node["landuse"="industrial"](around:${radius},${lat},${lng});way["landuse"="industrial"](around:${radius},${lat},${lng});relation["landuse"="industrial"](around:${radius},${lat},${lng}););out center tags 20;`; const res=await fetch("https://overpass-api.de/api/interpreter",{method:"POST",headers:{"Content-Type":"text/plain"},body:query,cache:"no-store"}); if(!res.ok) throw new Error(`Overpass failed: ${res.status}`); const data=await res.json(); return (data.elements??[]).slice(0,20).map((i:any)=>({id:String(i.id),name:i.tags?.name??"Sanayi alanı",lat:Number(i.lat??i.center?.lat),lng:Number(i.lon??i.center?.lon),kind:i.type})); }
